# Workloads

This directory is for benchmark prompt traces and prompt-length distributions.

Phase 7 starts with synthetic workloads in `bench/scenarios.py` so the benchmark
harness is runnable without external data. Real traces can be added here later as
JSONL files and referenced from scenario definitions.
